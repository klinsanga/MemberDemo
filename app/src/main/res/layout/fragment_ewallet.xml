<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context="th.co.omc.memberdemo.fragment.CommissionTabFragment">

    <th.co.omc.memberdemo.customview.CustomTabLayout
        android:id="@+id/ewallet_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar"
        android:background="#d16f16"
        android:elevation="6dp"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:textAppearance="?android:attr/textAppearanceSmall"
        app:tabBackground="@drawable/tab_color"
        app:tabIndicatorColor="@color/White"
        app:tabTextAppearance="@style/MyTheme"
        style="@style/CustomTabLayoutStyle"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/pagerEwallet"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/com_tab_layout"/>

</LinearLayout>